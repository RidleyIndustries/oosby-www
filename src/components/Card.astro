---
export interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`rounded-lg border border-oosby-border bg-oosby-surface-2 shadow-sm hover:bg-oosby-card-hover transition-colors ${className}`}>
  <slot />
</div>
