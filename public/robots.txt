# Robots.txt for Oosby

# Allow all crawlers (production)
User-agent: *
Allow: /

# Marketing site - allow all crawling
Disallow: /admin
Disallow: /api
Disallow: /.well-known

# Sitemap (will need to be generated)
Sitemap: https://www.oosby.com/sitemap.xml

# Disallow crawling of app area (authenticated routes)
Disallow: /app/
Disallow: /auth/
Disallow: /onboarding/

# Sitemap location (update when you add one)
# Sitemap: https://oosby.com/sitemap.xml
